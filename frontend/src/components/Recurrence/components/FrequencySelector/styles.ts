import { createStyles } from "@mui/material";

export default () => {
  return createStyles({
    frequency: {
      marginBottom: 10,
    },
    dropdown: {
      width: 100,
    },
    numberOfRepetitionsInput: {},
    weekDaysSelector: {},
  });
};
